// import { object, ObjectSchema, string } from 'yup';
// import { ICreatePostPayload, IUpdatePostPayload, IDeletePostPayload } from '../interfaces/post.interface';

// const createPostSchema: ObjectSchema<ICreatePostPayload> = object({
//   content: string().required('Content is a required field').min(1, 'Content must be at least 1 character')
// });

// const updatePostSchema: ObjectSchema<IUpdatePostPayload> = object({
//   id: string().required('ID is a required field'),
//   content: string().required('Content is a required field').min(1, 'Content must be at least 1 character')
// });

// const deletePostSchema: ObjectSchema<IDeletePostPayload> = object({
//   id: string().required('ID is a required field')
// });

// export { createPostSchema, updatePostSchema, deletePostSchema };
